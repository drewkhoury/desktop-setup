Vagrant.configure('2') do |config|

  config.vm.box = 'ubuntu/focal64'
  
  config.ssh.forward_x11 = true
  
  config.vm.define 'desktop_client_remote_work'
  config.vm.provider :virtualbox do |vb|
    vb.name = 'desktop_client_remote_work'
  end
  
end
